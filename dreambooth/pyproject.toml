[project]
name = "dreambooth"
version = "0.1.0"
description = "DreamBooth — Verbal dream description → visual dream journal with surrealist imagery"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.14.0",
    "python-dotenv",
    "fastapi",
    "uvicorn[standard]",
]
